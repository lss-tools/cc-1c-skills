{
  "name": "Общая форма есть в составе конфигурации, но не выгружена — предупреждение, не ошибка",
  "setup": "fixture:catalog-commonform-not-dumped",
  "params": {
    "objectPath": "Catalogs/Товары.xml"
  },
  "noSnapshot": "фикстура статична, meta-validate только читает и печатает — эталон зафиксировал бы саму фикстуру; проверяется stdout",
  "skipPlatformVerify": "фикстура — неполная выгрузка: общая форма есть в составе, файла нет намеренно; полная загрузка такого дерева невозможна",
  "expect": {
    "stdoutContains": "общая форма есть в составе конфигурации, файла формы в выгрузке нет",
    "stdoutNotContains": "[ERROR]"
  }
}
