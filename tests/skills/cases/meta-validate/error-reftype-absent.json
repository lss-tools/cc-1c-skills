{
  "name": "Ссылочный тип реквизита на объект, которого нет в составе конфигурации (Check 16)",
  "setup": "empty-config",
  "preRun": [
    {
      "script": "meta-compile/scripts/meta-compile",
      "input": { "type": "Catalog", "name": "ТестСсылкаБитая", "attributes": ["Контрагент: CatalogRef.НетТакогоСправочника"] },
      "args": { "-JsonPath": "{inputFile}", "-OutputDir": "{workDir}" }
    }
  ],
  "params": { "objectPath": "Catalogs/ТестСсылкаБитая" },
  "expectError": true,
  "noSnapshot": "meta-validate только читает и печатает — эталон зафиксировал бы выход preRun (meta-compile), а не проверяемого навыка; проверяется stdout",
  "expect": {
    "stdoutContains": "16. Ссылочный тип 'CatalogRef.НетТакогоСправочника' — объекта нет в составе конфигурации"
  }
}
